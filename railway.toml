[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn consultantt.wsgi --workers 2 --threads 4 --timeout 60 --log-file -"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
